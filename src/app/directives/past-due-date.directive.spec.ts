import { PastDueDateDirective } from './past-due-date.directive';

describe('PastDueDateDirective', () => {
  it('should create an instance', () => {
    const directive = new PastDueDateDirective();
    expect(directive).toBeTruthy();
  });
});
